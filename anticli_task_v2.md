**ЗАДАЧА ДЛЯ РОЯ АГЕНТОВ (ANTI-CLI TEST)**:
Вам поручено провести полный UX-аудит и рефакторинг двух ключевых экранов платформы Mizan Arabiya: `arabic/app/vocabulary/page.tsx` и `arabic/app/quiz/page.tsx`. 

**Контекст Бренда (КРИТИЧЕСКИ ВАЖНО):**
Платформа использует дизайн-систему **Mizan Editorial Brutalism**. Это НЕ "научно-фантастический индастриал" (никаких кислотных кибер-цветов, черных ховеров инверсии и технических лейблов типа MAT_X). 
- Это стиль **Дорогого печатного академического учебника**: элегантный, теплый, строгий.
- Цвета: `var(--mizan-sand)` (кремовый/бумага), `var(--mizan-deep)` (темно-серый/земляной), `var(--bg-card)` (папирус/белый).
- Формы: Жесткие углы (0px border-radius), строгие тонкие или жирные линии `border-[var(--mizan-deep)]`.
- Типографика: Гигантский арабский шрифт, а перевод на русский — классическим курсивом с засечками (serif italic), как сноски в словаре.

**Что нужно сделать:**
1. **Frontend Specialist**: Проанализируй текущий код `vocabulary` и `quiz`. Скорее всего, Лексика сейчас — это скучный список, а Квиз — базовые кнопки. Придумай элегантный "словарный" UX для லексики (например, крупная карточка-словарная статья с транскрипцией и примерами, а не просто список). Для Квиза — создай ощущение сдачи письменного экзамена на дорогой бумаге.
2. **Coder**: Полностью перепиши оба файла `arabic/app/vocabulary/page.tsx` и `arabic/app/quiz/page.tsx` в соответствии с планом Frontend Architect-а. Примени классы Mizan. 
3. Убедитесь, что все интерактивные элементы имеют состояния `hover` и `active`, использующие теплые оттенки `var(--mizan-sand)`, а не черные инверсии.

Ваша задача — переписать файлы и оставить их в рабочем состоянии.
